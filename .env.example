# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/pandara_samaja

# JWT Configuration
# IMPORTANT: Change this to a strong random string in production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# Google Drive Configuration (for photo uploads)
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# CORS Configuration (handled in app.js)
# Development origins: http://localhost:5000, http://127.0.0.1:5000
# Production origins: https://nikhilaodishapandarasamaja.in, https://www.nikhilaodishapandarasamaja.in
